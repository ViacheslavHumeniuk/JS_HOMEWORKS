'use strict';

const num1 = prompt("введіть перше число")
const num2 = prompt("введіть друге число")
const num3 = prompt("введіть третє число")

const average = (num1 + num2 + num3) / 3;

alert("Середнэ арифметичне чисел дорівнює " + average);