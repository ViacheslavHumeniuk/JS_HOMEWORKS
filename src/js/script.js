'use strict';

const firstNum = +prompt('ВЕДІТЬ ПЕРШЕ ЧИСЛО');
const secondNum = +prompt('ВЕДІТЬ ДРУГЕ ЧИСЛО');

const sumPlus = firstNum + secondNum ;
alert(firstNum + secondNum);

const sumMinus = firstNum - secondNum;
alert(firstNum - secondNum);

const sumMulti = firstNum * secondNum;
alert(firstNum * secondNum );

const sumDiv = firstNum / secondNum;
alert(secondNum / firstNum)